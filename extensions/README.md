# Fork overlay

Everything under `extensions/` is ours. Upstream `xai-org/grok-build` does not own this tree.

Use this directory for scripts, notes, and (later) a standalone Cargo workspace that path-depends on official crates. Do not register anything here in the generated root `Cargo.toml`.

Product extensions are not defined yet. Do not scaffold features here until they are.
